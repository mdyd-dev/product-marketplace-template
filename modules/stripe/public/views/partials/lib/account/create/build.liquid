{% parse_json data %}
{
  "external_id": "{{ current_user.id }}",
  "gateway_id": "{{ object.stripe_account.stripe_user_id }}",
  "state": "pending",
  "queued": false,
  "valid": true
}
{% endparse_json %}

{% liquid
  assign object = data
  return_rc object
%}
