{% liquid
  function_rc response = 'modules/stripe/lib/stripe_webhook/create/call'
  parse_json webhook
    include 'modules/stripe/lib/webhook_endpoints/create/build', response: response
  endparse_json
  if webhook.valid
    graphql result = 'modules/stripe/webhook_endpoint_create', args: webhook
    log result, type: 'webhooks/create model_create'
  endif
%}
