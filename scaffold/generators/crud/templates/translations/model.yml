model:
  new: New <%= modelName %>
  add: Add <%= modelName %>
  edit: Edit <%= modelName %>
  empty_state: You haven't added any <%= modelNamePlural %> yet. <br> Create your first one now!
  form:
<% attributes.forEach((attr) => { -%>
    <%= attr.name %>: <%= attr.nameHuman %>
<% }); -%>
