{% liquid

  unless id
    return null
  endunless

  graphql r  = 'items/search', id: id, limit: 1, withOrders: withOrders
  include 'lib/handle_graphql_response', r: r, name: 'queries/items/find'

  return r.items.results.first
%}
