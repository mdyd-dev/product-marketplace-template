{% parse_json object %}
  {
    "id": {{ context.current_user.id }},
    "first_name":    {{ object.first_name | json }},
    "last_name":    {{ object.last_name | json }}
  }
{% endparse_json %}

{% liquid
  return_rc object
%}
