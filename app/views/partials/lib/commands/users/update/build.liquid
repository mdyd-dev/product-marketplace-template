{% liquid
  if user.uuid == blank
    hash_assign user['uuid'] = '' | uuid
  endif
%}
{% parse_json object %}
  {
    "id":   {{ context.current_user.id }},
    "uuid": {{ user.uuid | json }}
  }
{% endparse_json %}

{% liquid
  return_rc object
%}
