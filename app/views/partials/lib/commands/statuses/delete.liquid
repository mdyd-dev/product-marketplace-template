{% liquid
  function object = 'lib/commands/statuses/delete/check',  object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'statuses/delete', selection: 'model_delete', object: object
  endif

  return object
%}
