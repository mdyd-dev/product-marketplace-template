{% liquid
  function object = 'lib/commands/line_items/delete/check', object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'line_items/delete', selection: 'model_delete', object: object
  endif

  return object
%}
