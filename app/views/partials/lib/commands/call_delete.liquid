{% liquid
  log command, type: 'command delete'

  assign check_path = 'lib/commands/' | append: command | append: '/check'
  function object = check_path, object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: command, selection: 'model_delete', object: object
  endif

  return object
%}
