{% liquid
  graphql consumers = 'events/consumers', name: object.type | fetch: "admin_liquid_partials" | fetch: "results"

  for consumer in consumers
    include consumer.path, event: object
  endfor
%}
