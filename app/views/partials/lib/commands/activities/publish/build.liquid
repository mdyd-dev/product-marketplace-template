{% parse_json object %}
  {
    "uuid": {{ uuid | json }},
    "type": {{ type | default: "custom" | json }},
    "ids":  {{ ids  | default: '1' | split: ',' }}
  }
{% endparse_json %}

{% liquid
  return_rc object
%}
