{% liquid
  function object = 'lib/commands/orders/delete/check',  object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'orders/delete', selection: 'model_delete', object: object
  endif

  return object
%}
