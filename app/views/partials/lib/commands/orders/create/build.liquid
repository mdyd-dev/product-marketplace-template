{% comment %}
  params: @item
          @current_user_id
{% endcomment %}

{% assign total = item.price | default: 0 %}

{% parse_json object %}
{
  "item_id": "{% print item.id %}",
  "buyer":   "{% print current_user_id %}",
  "seller":  "{% print item.owner.id %}",
  "total":    {% echo total %}
}
{% endparse_json %}

{% liquid
  return_rc object
%}
