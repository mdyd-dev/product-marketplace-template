{% liquid
  function object = 'lib/commands/contacts/delete/check',  object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'contacts/delete', selection: 'model_delete', object: object
  endif

  return object
%}
