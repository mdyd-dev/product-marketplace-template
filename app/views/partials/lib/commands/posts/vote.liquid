{%  liquid
  function object = 'lib/commands/posts/vote/build', object: object, profile: current_profile, post: post
  function object = 'lib/commands/posts/vote/check', object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'posts/update' object: object, selection: 'model_update'
  endif

  return object
%}
