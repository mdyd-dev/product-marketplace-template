{% liquid
  function object = 'lib/commands/categories/delete/check',  object: object

  if object.valid
    function object = 'lib/commands/execute', mutation_name: 'categories/delete', selection: 'model_delete', object: object
  endif

  return object
%}
