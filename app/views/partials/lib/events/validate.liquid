{% liquid
  assign valid = true

  if event.payload.type == '' or event.payload.type == blank
    log event, type: 'ERROR: event type is blank'
    assign valid = false
  endif

  export valid, namespace: 'event'
%}
