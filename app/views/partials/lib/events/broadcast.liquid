{% liquid
  graphql consumers = 'events/consumers', name: event.name | fetch: "admin_liquid_partials" | fetch: "results"

  for consumer in consumers
    include consumer.path, event: event
  endfor
%}
