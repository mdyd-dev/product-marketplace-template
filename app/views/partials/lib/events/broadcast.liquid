{% liquid
  graphql consumers = 'events/consumers', name: event.type | fetch: "admin_liquid_partials" | fetch: "results"

  for consumer in consumers
    include consumer.path, event: event
  endfor
%}
