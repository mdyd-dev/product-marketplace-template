{% liquid

  unless id
    return null
  endunless

  graphql r  = 'comments/search', id: id, limit: 1
  include 'lib/handle_graphql_response', r: r, name: 'queries/comments/find'

  return r.models.results.first
%}
