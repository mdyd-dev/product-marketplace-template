{% liquid
  if id == blank and email == blank and username == blank
    return_rc null
  endif

  graphql r  = 'users/search', id: id, email: email, username: username, limit: 1, with_token: with_token, with_followed: with_followed

  return_rc r.users.results.first
%}
