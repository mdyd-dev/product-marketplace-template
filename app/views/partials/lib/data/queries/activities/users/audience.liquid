{% liquid
  if id == empty
    return null
  endif

  graphql r = 'activities/users/audience', id: id, limit: 1000

  if r.errors
    log r, type: 'showme'
    break
  endif

  assign ids = r.users.results | map: 'id'

  return ids
%}
