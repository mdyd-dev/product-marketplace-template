{% liquid
  function query = 'lib/data/queries/items/search/build', limit: limit, owner: owner, params: context.params
  function items = 'lib/data/queries/items/search/execute', query: query

  return items
%}
