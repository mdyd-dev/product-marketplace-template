{% liquid
  function query = 'lib/data/queries/items/search/build', limit: limit, owner: owner, params: params
  function items = 'lib/data/queries/items/search/execute', query: query

  hash_assign items['query'] = query

  return items
%}
