{% liquid
  if uuid == blank and slug == blank and id == blank and user_id == blank
    return null
  endif

  graphql r  = 'groups/search', id: id, limit: 1, with_owner: with_owner

  return r.groups.results.first
%}
