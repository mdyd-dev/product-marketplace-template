{% liquid
  if uuid == blank and slug == blank and user_uuid == blank
    return null
  endif
  graphql r  = 'profiles/search', limit: 1, uuid: uuid, slug: slug, user_uuid: user_uuid

  return r.models.results.first
%}
