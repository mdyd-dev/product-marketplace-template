{% liquid
  include 'lib/users/validate', object: user
  if context.exports.object.valid
    include 'lib/users/save', object: context.exports.object
    include 'lib/sessions/create', object: context.exports.object

    session 'current_user_id', user.email
  endif
%}
