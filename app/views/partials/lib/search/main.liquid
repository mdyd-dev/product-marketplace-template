{% liquid
  graphql search_form = 'search/search_form' | dig: "models" | dig: "results" | first

  assign sort_options = 'lib.sort_options' | t
  assign search_form = search_form | add_hash_key: "sort_options", sort_options

  export search_form
%}
