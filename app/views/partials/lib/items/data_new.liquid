{% liquid
  assign icons = "alarm ambulance ban bandage calendar car yin-yang flame disc crop command compass copy stack star cut globe glass-full virus key trophy wand woman" | split: ' ' | sort
  export icons, namespace: 'lib'
  include 'lib/items/photos/presigned_url'
%}
