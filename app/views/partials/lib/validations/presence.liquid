{% comment %}
  params: @object
          @field_name
          @c
{% endcomment %}
{% liquid
  if object[field_name] == blank
    include 'lib/register_error', contract: c, field_name: field_name, key: key
  endif
  return c
%}
