{% liquid
  if user_id == blank
    return null
  endif

  if membership == null
    assign membership = false
  endif
  if followships == null
    assign followships = false
  endif

  function profile = 'lib/queries/profiles/find', user_id: user_id, id: id, followships: followships, membership: membership
  return profile
%}
