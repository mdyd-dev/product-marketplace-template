{% liquid
  assign valid = true

  if context.current_user.email != 'godot@tlen.pl' and context.current_user.email != 'piotr@near-me.com'
    include 'lib/flash/publish', error: 'app.errors.unauthorized'

    redirect_to '/sessions/new'
    break
  endif
%}
