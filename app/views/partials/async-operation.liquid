<h6>waiting for <code>{% print uuid %}</code> operation to accomplish</h6>

<script>
  setTimeout(
    function() {
      window.location = "{{ success }}"
    }, {{ wait | default: 500 }}
  )
</script>
