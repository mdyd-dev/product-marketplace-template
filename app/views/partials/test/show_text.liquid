Liquid tests
------------------------
{% liquid
  for contract in contracts
    render 'test/test_report_text', name: contract.test_path, contract: contract
  endfor
%}
------------------------
{% liquid
  if total_errors > 0
    assign result = 'Failed'
  else
    assign result = 'Success'
  endif
%}
{{result}}_{{test_name | strip }}
{% if total_errors > 0 %}
Total errors: {{ total_errors }}
{% endif %}
Finished in: {{ total_duration }}s
