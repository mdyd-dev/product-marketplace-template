{% parse_json order %}
{
  "lines": [
    {
      "item_id": 1,
      "price": 10
    }
  ]
}
{% endparse_json %}
{% liquid
  function_rc checkout_session = 'lib/commands/orders/calculate_payment/call', order: order

  if checkout_session.line_items[0].price_data.unit_amount != 1000
    include 'test/register_error', contract: contract, field_name: 'base', message: 'wrong amount'
  endif
%}
