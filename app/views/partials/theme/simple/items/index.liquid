<section class="text-gray-700 body-font">
  <div class="container mx-auto">
    <div class="flex flex-wrap -m-4 cards">
      {% for item in items.results %}
        {% include 'theme/simple/items/card', item: item %}
      {% endfor %}
    </div>
  </div>
</section>
