<section>
  <div class="flex flex-wrap -mx-4">
    {% for relation in list.results %}
      {% render 'theme/simple/profile/user_card', user: relation.r, current_user: current_user %}
    {% else %}
      <p class="container mx-4 my-8 text-lg">{{ 'app.no_following_found' | t }}</p>
    {% endfor %}
  </div>
</section>
