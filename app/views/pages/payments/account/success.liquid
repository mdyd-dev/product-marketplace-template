{% liquid
  include 'modules/stripe/lib/account/connect', params: context.params

  if context.exports.object.valid
    include 'theme/simple/payments/account/success'
  else
    include 'theme/simple/payments/account/failure'
  endif
%}
