{% liquid
  function_rc object = 'modules/stripe/lib/account/connect', params: context.params

  if object.valid
    include 'theme/simple/payments/account/success'
  else
    include 'theme/simple/payments/account/failure'
  endif
%}
