{% liquid
  function_rc account = 'modules/stripe/lib/payments/account'

  if account.id
    include 'theme/simple/payments/account/show', account: account
  else
    function_rc connect_url = 'modules/stripe/lib/account/connect_url'
    include 'theme/simple/payments/account/new', connect_url: connect_url
  endif
%}
