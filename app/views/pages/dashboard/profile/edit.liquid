{% liquid
  include 'lib/must_be_authenticated'

  function profile = 'lib/data/queries/profiles/find', uuid: context.session.user_profile_uuid
  graphql presigned_url = 'users/photos/presign' | dig: 'presigned'

  render 'theme/simple/dashboard/profile/edit', object: profile, presigned_url: presigned_url, context: context
%}
