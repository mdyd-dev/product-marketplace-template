---
metadata:
  breadcrumbs:
    - label: app.buyer
    - label: app.orders.list
---
{% liquid
  function current_user = 'lib/current_profile', user_id: context.current_user.id, required: true

  function orders = 'lib/data/queries/orders/search', limit: 100, buyer: current_user.id

  include 'theme/simple/dashboard/breadcrumbs', name: 'orders', total_entries: orders.total_entries, role: 'buyer'
  include 'theme/simple/dashboard/orders/index', orders: orders, role: 'buyer'
%}
