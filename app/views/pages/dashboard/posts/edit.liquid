---
metadata:
  breadcrumbs:
    - url: /dashboard/posts
      label: app.posts.list
    - label: app.edit
---
{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id, required: true
  function object = 'lib/data/queries/posts/find', id: context.params.id, creator_id: current_profile.id

  include 'theme/simple/dashboard/posts/edit', object: object
%}
