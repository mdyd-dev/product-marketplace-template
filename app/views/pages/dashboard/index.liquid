{% liquid
  function profile = 'lib/must_be_authenticated'
  include 'lib/validate_profile_completness', profile: profile

  include 'lib/commands/events/create', type: 'page_visited', actor_id: profile.id, object: context.location

  include 'theme/simple/dashboard/index', profile: profile
%}
