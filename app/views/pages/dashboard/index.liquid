{% liquid
  include 'lib/must_be_authenticated'

  function current_user = 'lib/data/queries/profiles/find', user_id: context.current_user.id
  include 'lib/validate_profile_completness', profile: current_user

  include 'lib/commands/events/create', type: 'page_visited', actor_id: current_user.id, object: context.location

  include 'theme/simple/dashboard/index', profile: current_user
%}
