{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id, required: true
  include 'lib/must_be_superadmin'

  function object = 'lib/data/queries/categories/find', id: context.params.id

  include 'theme/simple/categories/edit', object: object
%}
