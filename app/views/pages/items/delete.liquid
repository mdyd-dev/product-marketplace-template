---
method: post
---
{% liquid
  include 'lib/must_be_authenticated'

  include 'lib/commands/items/delete', id: context.params.id

  if context.exports.object.valid
    include 'redirect_to', url: '/dashboard/sell/items'
  else
    echo context.exports.object
  endif
%}

