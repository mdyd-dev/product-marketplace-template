{% liquid
  include 'lib/must_be_authenticated'

  function_rc filters = 'lib/search/filters', query: context.params
  include 'lib/queries/presigned_url'

  include 'theme/simple/items/new', lib: filters
%}
