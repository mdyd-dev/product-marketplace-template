---
slug: search
---
{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id, followships: true
  function items = 'lib/queries/items/search', params: context.params, limit: 21, status: 'published'
  function filters = 'lib/queries/search/filters', query: items.query

  include 'theme/simple/items/index', items: items, filters: filters, current_profile: current_profile
%}
