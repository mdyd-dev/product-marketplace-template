---
slug: groups
---
{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id, membership: true

  function list = 'lib/queries/groups/search', limit: 100

  include 'theme/simple/groups/cards', current_profile: current_profile, list: list
%}
