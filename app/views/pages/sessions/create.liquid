---
method: post
---
{% liquid
  include 'lib/sessions/validate', object: context.params.user

  if context.exports.object.valid
    include 'lib/sessions/create', object: context.exports.object

    if context.exports.object.valid
      redirect_to '/'
    else
      include 'theme/simple/sessions/new', object: context.exports.object
    endif
  else
    include 'theme/simple/sessions/new', object: context.exports.object
  endif
%}
