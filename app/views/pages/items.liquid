{% liquid
  assign id = context.params.slug2 | split: '-' | last

  function_rc item = 'lib/data/queries/items/find', id: id

  unless item.id
    render 'theme/simple/404', status: 404
    break
  endunless

  include 'theme/simple/items/show', item: item
%}
