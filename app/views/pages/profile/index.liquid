---
slug: profile
---
{% liquid
  include 'lib/must_be_authenticated'

  assign user_id = context.current_user.id
  function_rc user = 'lib/data/queries/users/find', id: user_id
  include 'theme/simple/profile/show', user: user
%}
