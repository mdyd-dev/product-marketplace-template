---
slug: off
---

{% liquid
  function_rc user = 'lib/data/queries/users/find', username: context.params.slug3
  unless user.id
    render 'theme/simple/404', status: 404
    break
  endunless

  function_rc current_user = 'lib/data/queries/users/find', id: context.current_user.id
  include 'theme/simple/profile/show', tab: 'following', user: user, users: users, current_user: current_user
%}
