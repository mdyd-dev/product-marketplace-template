{% liquid
  include 'lib/must_be_authenticated'

  function user = 'lib/data/queries/users/find', username: context.params.slug3
  unless user.id
    render 'theme/simple/404', status: 404
    break
  endunless


  include 'theme/simple/profile/show', user: user, tab: 'groups', list: groups
%}
