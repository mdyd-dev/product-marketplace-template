---
method: post
slug: order_statuses/create
---

{% liquid
  function_rc object = 'lib/commands/order_statuses/create', order_id: context.params.order_id, current_user_id: context.current_user.id, name: context.params.status

  if object.valid
      include "redirect_to", url: "/dashboard", notice: 'app.orders.status.updated'
  else
    echo object
    echo 'failed'
  endif
%}
