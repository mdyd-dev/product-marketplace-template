---
method: post
slug: order_statuses/create
---

{% liquid
  include 'lib/commands/order_statuses/create/call', order_id: context.params.order_id, current_user_id: context.current_user.id, name: context.params.status

  if context.exports.object.valid
      include "redirect_to", url: "/dashboard", notice: 'app.orders.status.updated'
  else
    echo context.exports.object
    echo 'failed'
  endif
%}
