mutation update_profile($id: ID!, $first_name: String, $last_name: String) {
  user: user_update(
    id: $id
    user: {
      properties: [
        { name: "first_name", value: $first_name }
        { name: "last_name", value: $last_name }
      ]
    }
  ){
    id
  }
}
