mutation create_user($email: String!, $password: String! $uuid: String!) {
  user_create(
    user: {
      email: $email
      password: $password
      properties: [
        { name: "uuid", value: $uuid }
      ]
    }
  ){
    id
    uuid: property(name: "uuid")
  }
}
